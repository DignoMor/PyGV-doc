# Documentation build dependency lock.
#
# These pins define the reproducible environment for the Sphinx build,
# including the runtime dependencies needed to import the paired code
# checkout and execute the example gallery. Regenerate deliberately and
# review changes before updating.
sphinx==7.4.7
pydata-sphinx-theme==0.15.4
myst-parser==4.0.1
sphinx-gallery==0.21.0
matplotlib==3.11.2
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
seaborn==0.13.2
pysam==0.24.1
pyBigWig==0.3.25
pyfaidx==0.9.0.4
pydantic==2.13.5
beautifulsoup4==4.15.0

# Test tooling used by the strict channel validation.
pytest==9.1.1
